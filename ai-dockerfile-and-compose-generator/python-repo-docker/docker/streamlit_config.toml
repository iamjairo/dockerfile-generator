# Streamlit server configuration for Docker deployments
# Placed at ~/.streamlit/config.toml inside the container

[browser]
gatherUsageStats = false

[server]
port = 8501
address = "0.0.0.0"
headless = true
enableCORS = false
enableXsrfProtection = true

[theme]
base = "dark"
